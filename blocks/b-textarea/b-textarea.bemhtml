block b-textarea {
    content: [
        {
            elem: 'inner',
            content: this.ctx.content,
            attrs: { name: this.ctx.name }
        }
    ]

    elem inner { tag: 'textarea' }
}