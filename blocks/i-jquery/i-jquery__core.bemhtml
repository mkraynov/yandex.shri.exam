block i-jquery, elem core, default: {
    return apply(
        this._mode = '',
        this.ctx = {
            block: 'b-page',
            elem: 'js',
            url: 'jquery-1.7.2.min.js'
        }
    );
}